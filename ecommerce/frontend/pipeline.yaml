name: frontend-deploy

build:
  image: node:20-alpine

steps:

  install:
    commands:
      - npm install

  build:
    commands:
      - npm run build

  deploy:
    commands:
      - echo "Build completed"
